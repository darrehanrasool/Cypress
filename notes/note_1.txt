Cypress is a Javascript E2E web UI automation.
Javascript runs directly inside the web browser.
Cypress    runs directly inside the web browser.
There are certain limitations of Cypress :
we can't switch between browser windows.
Limited iframe support.
Parallel execution in cypress cloud.

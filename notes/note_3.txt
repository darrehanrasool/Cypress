what are locators ? via locators we can target DOM elements eg id="email".
cypress locators have 3 methods :
cy.get("");     : searching element via tag  globally
cy.contains("") : searching element via text globally
cy.find("")     : searching child elements only 
